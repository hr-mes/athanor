# nouveau and nova_core must never claim an NVIDIA GPU in the NVIDIA image variants
# (doc_system_image.md, S4 and S5). negativo17 ships the same lines, RPM Fusion ships none.
blacklist nouveau
blacklist nova_core
