# iso-v0: crate fuori dalla build di boot (NEXT.md BLOCCO 2). Tornano nel DAG con la v1.
# Un nome per riga; letto da scripts/verify.py (check "shipped").
athanor-mesh-bus
athanor-cluster-mesh
athanor-mesh-sync
athanor-cloud-rs
athanor-compositor
athanor-hypervisor-daemon
athanor-net-unikernel
athanor-telemetry
athanor-init-oracle
athanor-sysmon-ebpf
athanor-mdm-rs
athanor-greeter
# Out of the system image since 2026-09-17: the source stays, the RPM does not ship.
# athanor-store-rs ran `flatpak install` for any D-Bus caller without the polkit check
# its policy declares, and verified package signatures against a caller-supplied key;
# cosmic-store covers the store until it is redesigned.
athanor-store-rs
# athanor-settings-rs "signed" its CRDT writes with a Dilithium key generated for each
# write and passed a Cloudflare API token to curl on the command line; cosmic-settings
# covers settings until the application is redesigned.
athanor-settings-rs
# athanor-daemon-rs was D-Bus-activated on the session bus, outside its hardened system
# unit, and claimed os.athanor.Bedrock and the athanor portal name there; nothing in the
# COSMIC session uses it.
athanor-daemon-rs
# athanor-dock has no unit since the COSMIC dock replaced it; the crate stays a library
# of athanor-shell-rs, only its standalone binary leaves the image.
athanor-dock
